```

---

```markdown

Created by: **Madhusha M. Gunasekara**


---

### Usage in React / Electron
```tsx
const displaySpeed = useSmoothSpeed(apiSpeed, 1500);
return <h1>{displaySpeed} km/h</h1>;
```

---

### Usage in React Native
```tsx
const displaySpeed = useSmoothSpeed(apiSpeed, 1200);
return <Text>{displaySpeed} km/h</Text>;
